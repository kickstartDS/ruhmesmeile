// Registers the `base.lightbox-image` component used by image-text (and
// gallery) so an image can be opened in the full-screen lightbox.
import "@kickstartds/base/lib/lightbox-image/lazyLightboxImage";
