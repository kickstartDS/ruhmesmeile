// Registers the `content.count-up` component used by the stats component: it
// animates the numbers once they scroll into view. Without this the stats
// render their final figures immediately (or not at all).
import "@kickstartds/content/lib/count-up/lazyCountUp";
