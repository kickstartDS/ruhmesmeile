node_modules
npm-debug.log
.next
**/.env.local
**/.env.sh
**/*.pem
# The website image builds the design system from source; a host-side dist is
# never an input (see packages/website/Dockerfile).
packages/design-system/dist
