version: 2.1

defaults: &defaults
  docker:
    - image: cimg/ruby:3.4.7
  resource_class: large

# aliases:
#   - &save-cache-yarn
#     key: yarn-{{ checksum "yarn.lock" }}-{{ checksum ".circleci/config.yml" }}
#     paths:
#       - .yarn-cache
#   - &restore-cache-yarn
#     keys:
#       - yarn-{{ checksum "yarn.lock" }}-{{ checksum ".circleci/config.yml" }}
#       - yarn-{{ checksum "yarn.lock" }}
#   - &save-cache-node
#     key: node-v1-{{ checksum "yarn.lock" }}-{{ checksum ".circleci/config.yml" }}
#     paths:
#       - node_modules
#   - &restore-cache-node
#     keys:
#       - node-v1-{{ checksum "yarn.lock" }}-{{ checksum ".circleci/config.yml" }}
#       - node-v1-{{ checksum "yarn.lock" }}
#       - node-v1-

jobs:
  install:
    <<: *defaults
    steps:
      - checkout
      # - restore_cache: *restore-cache-yarn
      # - restore_cache: *restore-cache-node
      - run:
          name: Install Kamal
          command: gem install kamal
      # - save_cache: *save-cache-yarn
      # - save_cache: *save-cache-node

  deploy:
    <<: *defaults
    steps:
      - checkout
      # - restore_cache: *restore-cache-node
      # - attach_workspace:
      #     at: .
      - run:
          name: Build & Deploy Website
          command: |
            kamal deploy

workflows:
  release:
    jobs:
      - install

      - deploy:
          requires:
            - install
